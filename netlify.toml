[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA-Fallback (für später, wenn Client-Routing dazukommt). Schadet aktuell nicht.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
